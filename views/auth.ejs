<md-content ng-controller="AuthController">
    <div>
        <div>
            <h3><label>ID: <input type="text" ng-model="person.id"  /></label>
                <label>Key: <input type="text" ng-model="person.key"  /></label>
            </h3>
        </div>
        <md-button class="md-raised md-accent" ng-click="validate(person)">Validate</md-button>
        <input ng-show="items_fetched" type="button" ng-click="deleteselected()" value="Delete Selected" />
    </div>
</md-content>